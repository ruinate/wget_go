# WGET_GO


##  代码结构

```text
├── core
│   ├── get.go
│   └── set.go
├── go.mod
├── go.sum
├── main.go
├── Makefile
├── README.md
└── wget_go
```

## 使用说明

```text
Display one or many resources

Usage:
  wget_go [command]

Available Commands:
  completion  Generate the autocompletion script for the specified shell
  get         获取文件
  help        Help about any command
  set         设置下载目录

Flags:
  -h, --help      help for wget_go
  -v, --version   version for wget_go

Use "wget_go [command] --help" for more information about a command.
```